# ── Docker profile: services run inside Docker network ────────────────────────
# Must use Docker internal DNS — localhost would resolve to the container itself
spring.kafka.bootstrap-servers=kafka:29092
